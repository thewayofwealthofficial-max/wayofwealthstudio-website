# Lead-magnet PDFs go here

Drop the real files here. Filenames must match what the landing pages link to.

| Page | Expected file | Status |
|---|---|---|
| `/budget-tracker` → `/thanks/budget-tracker` | `budget-tracker.pdf` | **Joel to upload** |

When you upload, the path stays `/lead-magnets/budget-tracker.pdf` (Astro serves `public/` at site root).

This README is harmless if it ships — it's just a marker.
